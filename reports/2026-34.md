# Week 2026-34 (Sun 2026-08-23) — ANALYZE + BUILD

## Session-start recovery
Local `main` was 1 commit behind `origin/main` (HEAD detached at the correct tip, `caa5cb7` —
FIND #28's WordPress-queue + Queued-section byte-ceiling relief, committed but never fast-
forwarded onto `main` by the prior session). `merge-base` confirmed a pure fast-forward, no
divergence — ff-merged and pushed. Same recurring pattern as every prior session start
(LEARNINGS #32/#39 lineage); still unresolved at the infrastructure level, still zero data at
risk each time.

## ANALYZE
Pre-launch phase (OPERATIONS.md §4) still applies: dataset readiness is the metric, not traffic.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs,
  confirmed live via GitHub API) — unchanged since 08-16, expected pre-launch.
- **Site traffic:** `reports/stats/total.json` snapshot (2026-08-22T21:30 UTC) shows 3 hits
  over the visible window, 0-2/day — consistent with operator/owner checks only. Snapshot is a
  week stale relative to today (08-23); the daily Analytics-snapshot Action has been green
  through 08-22 per Actions history, so the data itself is trustworthy, just narrower than
  usual. No collection-page or column kill/promote decision is due (SEO-tail gate: no kills
  before week 10 of real traffic — nowhere close).
- **CI health:** `CI & Deploy` and `Analytics snapshot` both green on every run 08-17 through
  08-22 (confirmed via workflow-run history) — no cadence gaps this cycle.
- **Launch gate status:** 36 apps tracked (32 live + 4 pending-second-qa) vs. the ≥100-app
  gate — still far off, no gate decision to make this run.
- **Governance byte budgets:** DECISIONS.md was at 7932/8000 (68 bytes headroom) — too tight
  to add this run's entry without first compacting. Archived the 8 decisions from 07-24 through
  08-09 to `reports/archive/decisions-log.md` (full text preserved there), left one-line
  summaries inline for cadence-gap scanning — same pattern already used for LEARNINGS/backlog.
  7932→4182 bytes before this run's own entry; 5558 after.

## BUILD (backlog drain)
**Fresh-eyes re-QA'd the 4 `pending-second-qa` apps** (Coolify, Prometheus, docker-mailserver,
Wazuh) with an independent agent that did not build or verify them originally. docker-mailserver
and Wazuh clean on all 14 Known Defect Classes, zero changes. Coolify: scope wording on the
RAM/CPU minimum overstated specificity (implied quick-install-script-only when the doc's
"Before You Begin" section actually covers both install paths) — reworded. Prometheus:
`docker.size_mb` had drifted 102→104 (image was repushed 2026-08-18, after the original
2026-08-16 retrieval) — re-derived from the live manifest, retrieved date updated. **All 4
promoted to live** (32 live total).

**Harvested a new 4-app batch** from the verified queue, picked for score + simplicity (no
unresolved SERVICES-enum blocker — Dokploy, the actual top-scoring item at 18/20, needs
`traefik` which has no enum slot, so it was passed over this batch, LEARNINGS #64): Kestra
(18/20), code-server (17/20), Ollama (17/20), Dockge (17/20). Notable: Ollama has no official
base RAM/CPU figure anywhere (usage is entirely model-dependent, per-model sizes live on
individual ollama.com/library pages) — correctly left `no_official_figure` rather than encoding
one model's footprint as the app-level spec (Open WebUI precedent). Kestra's Postgres/MySQL
requirement is a genuine either/or — modeled as two `required:false` deps with an explanatory
`note` field rather than flattening into a required+optional pair (Defect Class #11).

**Independent verification** re-fetched every source live for all 4 (kestra.io/coder.com/
ollama.com were egress-blocked from the sandbox, same as the harvester — citations rest on
GitHub-mirrored docs, cross-confirmed, not canonical-domain-confirmed for those three). Found
and fixed one stale claim: Ollama's `no_official_figure` note described upstream issue #2418 as
"Open" — it is actually closed without resolution; note corrected, and the verifier
independently re-checked the current `.mdx` docs tree to confirm the underlying "no figure
exists" claim still holds regardless of the issue's state. `docker.size_mb` re-derived and
exact-matched for all 4 (including Dockge's 3-arch amd64/arm64/armv7 manifest, specifically
checked rather than assumed). Moved all 4 to `pending-qa`.

**Independent QA** (did not harvest or verify) re-derived every figure and `docker.size_mb`
again from scratch for all 4. Zero defects found. Explicitly ruled on the Kestra OR-dependency
question the verifier had flagged as "worth normalizing": the two-`required:false`-deps
structure matches the dataset's existing convention (grafana/keycloak/nextcloud all model
multi-backend DB choice the same way) — kept as-is, not a defect. Separately flagged a real but
out-of-scope gap: `build.mjs` never renders the OR relationship in prose for *any* multi-backend
app in the dataset, not just Kestra — a pre-existing template gap, logged as LEARNINGS #65 for a
future page-template pass, not fixed this run (QA's scope is data, not templates). Per the
unattended-run rule, all 4 land at **pending-second-qa** (not live) — next run's fresh eyes give
the final settle.

**Backlog housekeeping:** `backlog/opportunities.md` updated (Queued → In pipeline for the 4
built apps, Shipped 28→32, Dokploy's traefik-enum blocker noted). LEARNINGS.md gained #64
(traefik enum gap) and #65 (OR-dependency prose gap); both entries specific and downstream-
actionable, no compaction needed this cycle (4006/8000 bytes).

## Evidence
- Tests: **57/57 pass** at every checkpoint (`node --test tests/*.test.mjs`), including after
  each of this run's 6 commits.
- Commits (chronological): `ffe1662` (session recovery ff-merge, pushed as part of prior
  session's un-landed work), `f80ae6d` (BUILD: re-QA settles Coolify/Prometheus/docker-
  mailserver/Wazuh to live), a governance commit compacting DECISIONS.md, `c9bd62a` (BUILD WIP:
  harvest Kestra/code-server/Ollama/Dockge), `859ace3` (BUILD WIP: independent verification,
  1 fix), `33a96df` (BUILD: independent QA clears all 4, backlog + LEARNINGS updated).
- Dataset: 36 entries tracked — 32 live (+4 this run: Coolify/Prometheus/docker-mailserver/
  Wazuh), 4 pending-second-qa (Kestra/code-server/Ollama/Dockge, next run's fresh eyes settle
  them).
- Governance: LEARNINGS.md 4006/8000, DECISIONS.md 5558/8000, backlog/opportunities.md
  6499/10000 — all comfortably under budget after this run's compaction + additions.
- Blocked, not built: Dokploy (18/20, top of queue) deferred — needs a `traefik` SERVICES-enum
  slot that doesn't exist yet (LEARNINGS #64). Remaining Queued items (Zammad, Ghost, Mastodon,
  Lemmy, Netdata, Portainer, Wekan, Directus, Supabase, Twenty CRM, Formbricks, Zigbee2MQTT,
  TriliumNext, SearXNG, Stirling-PDF, Metabase, Odoo CE, License column, Technitium DNS Server,
  Mailu, Penpot) carry forward untouched — pacing, not a blocker.

## LEARNINGS added
#64 (traefik has no SERVICES enum slot — blocks Dokploy, resolve before its BUILD turn).
#65 (`build.mjs` never renders a DB-OR dependency's alternative relationship in prose, for any
multi-backend app in the dataset — real gap, future template-touching batch should fix it).
