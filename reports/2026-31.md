# Week 2026-31 (Thu 2026-07-30) — ANALYZE + BUILD

## ANALYZE
Pre-launch phase (OPERATIONS.md §4): the metric is dataset readiness, not traffic — applied
cleanly, no traffic-driven decisions this cycle.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs).
  Nothing to act on; expected pre-launch.
- **Site traffic (reports/stats/):** 63 total hits since 07-21, last snapshot 2026-07-28
  (2 days stale) — consistent with operator/owner checks, not real visitors. No collection/
  column kill evaluated (OPERATIONS.md requires real site traffic first; none exists yet).
- **CI health:** `CI & Deploy` green on every run this week. `Analytics snapshot` failed 4 of
  the last 6 scheduled runs (curl exit 22 against the GoatCounter API), succeeding
  intermittently rather than being fully broken — added `--retry 3` with backoff to both curl
  calls; can't diagnose further from this cloud session (goatcounter.com egress-blocked).
  Flagged again per LEARNINGS' standing instruction to keep flagging until resolved.
- **Session-start repo-integrity check:** local `main` was 16 commits behind `origin/main` —
  investigated before assuming a fork; `git merge-base` confirmed origin was a direct
  descendant (a prior session had pushed everything; origin had just moved one commit further
  with today's FIND run #6). Fast-forwarded, nothing lost. Full detail in DECISIONS.md.
- **Launch gate status:** 17 live + 4 pending-second-qa (21 total tracked) vs. the ≥100-app
  gate — still far off; no gate decision to make.

## BUILD (backlog drain)
**Settled from last cycle:** independent fresh-eyes re-QA cleared Discourse, Zulip, and
Rocket.Chat — all three now `live` (17 apps live). One non-blocking fix: Discourse's
`docker.size_mb` had drifted 1144→1173 (same rolling-`latest`-tag pattern as the Vaultwarden
SEV-1 from AUDIT #1) — corrected, changelog entry added, plus Vaultwarden's own missing
changelog entry from that earlier fix (retroactively logged).

**Built this cycle: OpenProject, Plausible CE, Linkwarden, Open WebUI** — the entire verified
backlog queue that wasn't blocked on egress. Independent verification found zero discrepancies
on any of the four (unlike the 07-26 batch, which the verifier had blocked once). Independent
QA likewise found zero defects across all 12 Known Defect Classes; one non-blocking note for a
future BUILD pass (Linkwarden's anecdotal 4GB figure visually reads like a formal "recommended"
figure despite being disclosed as informal in its quote/scope — not a current-convention
violation). All four now carry `pending-second-qa` (unattended-run rule) — next run's fresh-eyes
QA settles them.

One schema addition: `meilisearch` added to the deps SERVICES enum (Linkwarden's optional
search dependency, sourced from its official compose + env-var docs — same precedent as the
earlier memcached addition, not counted against the one-schema-change pacing since it's a
sourced enum extension, not a new field).

**Still blocked, unbuilt:** Portainer, Netdata, PeerTube, Vikunja — confirmed egress
constraint (no GitHub-hosted mirror), unchanged since 07-26.

**Two site fixes shipped alongside:** a real test gap (site-invariants' quote-display check
didn't escape `>`, false-failing OpenProject's verbatim quote) and a real search bug
(name-matching was verbatim, so "rocket chat" missed "Rocket.Chat" — both this run's QA agent
caught it, now fixed with punctuation-stripped comparison on both sides).

## Evidence
- Tests: **42/42 pass**, `node --test tests/*.test.mjs`, 0 fail, re-run clean after every fix.
- Commits (chronological): `84245ee` (analytics-snapshot retry), `2b7d2b2` (re-QA settle
  Discourse/Zulip/Rocket.Chat to live), `ad9a62e` (search punctuation fix), `4585c89` (BUILD:
  4 new apps, pending-qa, + test-escape fix), plus this report's commit (QA settle to
  pending-second-qa + governance updates).
- Dataset: 17 live, 4 pending-second-qa (OpenProject, Plausible CE, Linkwarden, Open WebUI) =
  21 entries tracked.
- Blocked, not built: Portainer, Netdata, PeerTube, Vikunja (egress); GPU column,
  community-figures column (schema pacing, unchanged from prior cycles).

## LEARNINGS added
#32 (stale local branch ref vs. real fork — check merge-base first), #33 (test escape sets
must mirror the renderer's exactly), #34 (markdown emphasis stripping is safe in verbatim
quotes, nothing else is). LEARNINGS.md compacted (two 07-26 entries graduated to one-liners)
to stay under its byte budget.

# 2026-08-02 (Sun) — ANALYZE + BUILD

## ANALYZE
Pre-launch phase still applies (dataset readiness, not traffic).
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues — unchanged, expected pre-launch.
- **Site traffic:** 75 total hits per `reports/stats/total.json` (up from 63 a week ago) —
  still consistent with operator/owner checks, nothing traffic-driven to evaluate.
- **Session-start repo-integrity check:** local `main` was 3 commits behind `origin/main`
  after an initial `git checkout main` (the checkout itself doesn't fast-forward) — caught
  before doing any work off a stale tree, `git merge --ff-only origin/main` resolved it.
  Distinct near-miss from LEARNINGS #32: that one was a detached-HEAD-vs-stale-branch
  confusion; this one is "checkout doesn't pull." Logged as new LEARNINGS entry.
- **CI health:** `CI & Deploy` green on every run since last report. **Analytics snapshot has
  been green 3 runs straight (07-30, 07-31, 08-01)** since the `--retry 3` fix landed — the
  standing "keep flagging every run" instruction from LEARNINGS can retire; this is now a
  confirmed fix, not a hopeful one.
- **Launch gate status:** 21 apps tracked (17 live + 4 pending-second-qa) vs. the ≥100-app
  gate — still far off, no gate decision to make.

## BUILD (backlog drain)

