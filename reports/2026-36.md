# Week 2026-36 (Wed 2026-09-02) — ANALYZE + BUILD

## Session-start recovery
`git branch --show-current` was empty (detached HEAD) at session start — the SKILL.md
invariant #9 pattern recurred again. `git branch -f main HEAD && git checkout main` then
`git fetch origin main`: local `main` and `origin/main` both resolved to `92dcc55` (FIND #38)
— a pure identity match, no divergence, no ff-merge needed, zero commits at risk. 65/65 green
confirmed before touching anything.

## ANALYZE
Pre-launch phase (OPERATIONS.md §4) still applies: dataset readiness is the metric, not traffic.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs,
  confirmed live via GitHub API) — unchanged since bootstrap, expected pre-launch.
- **Site traffic:** `reports/stats/total.json` snapshot (2026-09-01T23:14 UTC) shows 6 hits
  total across the visible 9-day window (08-25 through 09-02), concentrated on 08-25 (2) and
  09-01 (4), all other days 0. Consistent with operator/owner checks only. No collection-page
  or column kill/promote decision is due (SEO-tail gate: no kills before week 10 of real
  traffic; pre-launch gate doesn't consider traffic at all).
- **Launch gate status:** 44 apps tracked (40 live + 4 pending-second-qa) vs. the ≥100-app
  gate — still far off, no gate decision to make this run.
- **Governance byte budgets** (checked per LEARNINGS #73, at session start): LEARNINGS.md
  7333/8000 (667B headroom), DECISIONS.md 7668/8000 (332B headroom — under the ~500B proactive
  threshold, compacted this run before adding new entries), backlog/opportunities.md 9047/10000
  (953B headroom — watched through this run's edits).
- No traffic-driven or double-down decisions this run — same conclusion as prior weeks.

## BUILD (backlog drain)
**Fresh-eyes re-QA'd the 4 `pending-second-qa` apps** (Homebox, Mailcow, Metabase, Technitium
DNS Server) with an independent agent that did not build, verify, or QA them previously. It
live-refetched every `source_url`/`evidence_url`, independently re-derived every
`docker.size_mb` from live registry manifests (GHCR anonymous-token manifest API for
Homebox/Mailcow, Docker Hub API for Metabase/Technitium), and checked all 16 Known Defect
Classes per entry — with particular attention to Class #15 (representative-image ambiguity)
on Mailcow, the defect class Mailcow itself motivated. **Zero defects found on all 4** — every
cited quote still verbatim-present at its live source, every docker size matched exactly (no
drift), every arch list matched, deps confirmed against live compose files. All 4 promoted to
`live` (44 live, 0 pending-second-qa). One cosmetic (non-data) observation logged: Mailcow's
`docker.note` Wazuh comparison is a slightly loose analogy — not fixed, doesn't affect
correctness.

**Harvested a new 4-app batch** from the verified queue (top scores, no schema blocker —
Dokploy, still top-scoring at 18/20, remains deferred pending a `traefik` SERVICES-enum slot,
LEARNINGS #64, now many cycles unresolved): Pangolin (18/20), Ghost (17/20), Activepieces
(17/20), Jitsi Meet (17/20).

**Independent verification** (agent distinct from the harvester) re-fetched every source live
and independently recomputed every `docker.size_mb`/arches from registry manifests. Found and
fixed 3 defects: Ghost's `deps[].note` cited a docker-install-guide sentence that no longer
exists anywhere in `TryGhost/docs` (confirmed via a repo-wide code search — a fabricated/stale
citation, Defect Class #8, same failure family as LEARNINGS #71/#79) — replaced with a genuine
verbatim quote from `faq/supported-databases.mdx`; Activepieces' production-table quote had an
ASCII `--` where the live source uses an em dash (byte-level quote drift); Jitsi Meet's `:stable`
tag citation was missing its source markdown backticks (cosmetic). Pangolin: zero defects.
Notably the verifier corroborated two of the harvester's own self-flagged judgment calls as
sound rather than shortcuts: Jitsi Meet's absent CPU minimum (only a Prosody per-process
ceiling exists, not a system floor) and Activepieces' quickstart-vs-production min/rec split.

**Independent QA** (3rd agent, distinct from harvester and verifier) re-derived every figure,
dep, and docker size from scratch for all 4 — zero further defects. Confirmed on the actual
built pages: figures/quotes/dates render, `no_official_figure` states absence plainly with a
working evidence link (Ghost's ram_rec/cpu, Jitsi Meet's cpu_min), `docker.note`/`deps[].note`
both render in HTML (not just JSON), collection-page membership is CI-mechanically diffed
against the data (not eyeballed), 320px viewport stays legible, no `innerHTML`-with-data DOM
safety violation. Found one open regression gap: the CI suite asserted `docker.note` renders
(Defect Class #15's fix) but had no equivalent assertion for `deps[].note` — same silent-drop
risk, 11 live apps currently carry one. Not a live defect (all 11 currently render correctly),
but closed the gap directly: added a mirrored CI assertion (`c28fb85`), full suite re-confirmed
green. All 4 land `pending-second-qa` per the unattended-run rule (48 tracked: 44 live + 4
pending-second-qa).

**Backlog housekeeping:** `backlog/opportunities.md` updated (Shipped 40→44, Queued → In
pipeline for the 4 new apps, all 4 removed from Queued). `reports/LEARNINGS.md` compacted
(7333→6900 bytes): removed 2 entries (#71 Dify quote-fabrication, #72 Activepieces severity-
softening) that were already fully preserved verbatim in `reports/archive/learnings-compacted.md`
— no data lost, just freed active-file headroom.

## Evidence
- Tests: **69/69 pass** at every checkpoint (`node --test tests/*.test.mjs`), including after
  every commit this run.
- Commits (chronological): `4a936e7` (ANALYZE + DECISIONS.md compaction 7668→2112), `ece6f53`
  (fresh-eyes QA promotes Homebox/Mailcow/Metabase/Technitium DNS Server to live), `6ad8a7b`
  (BUILD WIP: harvest Pangolin/Ghost/Activepieces/Jitsi Meet), `cdb21d8` (BUILD WIP: independent
  verification, 3 defects fixed), `c28fb85` (QA: closes deps[].note rendering-gap regression
  test), plus this run's final commit promoting the 4 to pending-second-qa.
- Dataset: 48 entries tracked — 44 live (+4 this run: Homebox/Mailcow/Metabase/Technitium DNS
  Server), 4 pending-second-qa (Pangolin/Ghost/Activepieces/Jitsi Meet, next run's fresh eyes
  settle them).
- Governance: LEARNINGS.md 6900/8000 (after compaction), DECISIONS.md ~2400/8000 (after
  compaction + this run's entry), backlog/opportunities.md 8241/10000 — all comfortably under
  budget.
- Blocked, not built: Dokploy (18/20, top of queue) still deferred — needs a `traefik`
  SERVICES-enum slot (LEARNINGS #64, unresolved many cycles running). Remaining Queued items
  (Zammad, Mastodon, Lemmy, Netdata, Portainer, Wekan, Directus, Supabase, Formbricks,
  Zigbee2MQTT, TriliumNext, SearXNG, Stirling-PDF, License column, Odoo CE, Mailu, Penpot,
  WordPress, Langfuse, wg-easy, Dify, Stalwart, Monica) carry forward untouched — pacing, not
  a blocker.

## LEARNINGS
No new numbered entry this run — the Ghost fabricated-quote catch and the deps[].note
regression-test gap were both handled entirely within existing mandatory process (independent
verification, independent QA) with no procedural gap to close, so no downstream-behavior-
changing learning was warranted per the "changes nothing = not a learning" rule. Compacted 2
already-superseded entries (#71, #72) out of the active file (fully preserved in archive).

