# Week 2026-30 (Sun 2026-07-26) — ANALYZE + BUILD

## ANALYZE
Pre-launch phase (OPERATIONS.md §4): the metric is dataset readiness, not traffic — applied
cleanly, no traffic-driven decisions this cycle.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs).
  Nothing to act on; expected pre-launch.
- **Site traffic (reports/stats/):** 26 total hits since 07-17, mostly single-digit days —
  consistent with operator/owner checks, not real visitors. No collection/column kill
  evaluated (OPERATIONS.md requires real site traffic first; none exists yet).
- **CI health:** `CI & Deploy` green on every run through this week (last: run 30179518844,
  success). `Analytics snapshot` **failed 2026-07-25** (curl exit 22 against the GoatCounter
  API) and has not been retried since — stats are 1 day stale as of this report. Flagged to
  owner; cloud sessions can't reach goatcounter.com to diagnose further (egress policy), and
  the two CI workflows don't cross-gate so this was invisible without checking Actions
  directly (LEARNINGS #22).
- **Launch gate status:** 14 live + 3 pending-second-qa (17 total) vs. the ≥100-app gate —
  still far off; no gate decision to make.

## BUILD (backlog drain)
Attempted the full 7-app verified queue; scoped down to 3 on a confirmed infrastructure
constraint (tested, not assumed): Docker Hub's API, GHCR's registry API, and
raw.githubusercontent.com are reachable from this cloud session, but docs.portainer.io,
learn.netdata.cloud, docs.joinpeertube.org, and vikunja.io all hard-block (curl exit 56,
connection-level — confirmed with both WebFetch and direct Bash curl). Portainer, Netdata,
PeerTube, and Vikunja remain `queued`, unbuilt, pending a local session or an owner-provisioned
network allowlist.

**Built this cycle: Discourse, Zulip, Rocket.Chat** — harvested, independently verified
(verifier BLOCKED Zulip on first pass over a silently-dropped `memcached` dependency and a
miscited docker-image source; both fixed and re-verified clean), then independently QA'd
(QA BLOCKED on one real reader-facing defect — a collection page's fixed copy claiming
"no Postgres, no Redis" went false once Discourse's bundled-but-real Postgres/Redis joined
that collection; fixed the copy, plus two non-blocking nits). Full detail in DECISIONS.md.

All three now carry status `pending-second-qa` (unattended-run rule) — the next run's fresh-eyes
re-QA settles them, not this one grading its own fix.

## Evidence
- Tests: **38/38 pass**, `node --test tests/*.test.mjs`, 0 fail, re-run clean after every fix.
- Commits: `dddb77f` (harvest + independent verification + governance updates), plus this
  report's own commit (QA fixes + weekly report).
- Dataset: 14 live, 3 pending-second-qa (Discourse, Zulip, Rocket.Chat) = 17 entries tracked.
- Schema change this batch (paced at one per batch): added `memcached` to the deps enum.
- Blocked, not built: Portainer, Netdata, PeerTube, Vikunja (egress); GPU column,
  community-figures column (schema pacing, unchanged from prior cycles).

## LEARNINGS added
#18 (fetch capability is domain-specific, not blanket), #19 (image-embedded requirement
tables), #20 (verifier caught a silently-dropped dep — schema gaps aren't a license to drop
data), #21 (backlog figures drift, reconfirmed), #22 (Analytics snapshot failure, invisible
without checking Actions), #23 (fixed collection copy can go stale on a new member), #24
(quote field must stay pure transcription). LEARNINGS.md compacted (six graduated 2026-07-24
entries rolled into one-liners) to stay under its byte budget.
