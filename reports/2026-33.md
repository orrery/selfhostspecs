# Week 2026-33 (Wed 2026-08-12) — ANALYZE + BUILD

## Session-start recovery
Local `main` branch ref was 1 commit behind `origin/main` (HEAD was detached at the correct
tip, same recurring pattern as 07-30/08-09 — the prior session ends detached and the local
branch ref is never fast-forwarded). `git checkout -B main origin/main` re-pointed it; no
divergence, no data at risk (standing session-start check, LEARNINGS #32/#39 lineage).

## ANALYZE
Pre-launch phase (OPERATIONS.md §4) still applies: dataset readiness is the metric, not traffic.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs)
  — unchanged, expected pre-launch.
- **Site traffic:** `reports/stats/total.json` (snapshot 2026-08-11T21:58 UTC) shows 19 hits
  in the last 30 days, low single digits daily — consistent with operator/owner checks only,
  nothing traffic-driven to evaluate. No collection-page or column kill/promote decision is due
  (SEO-tail gate: no kills before week 10 of real traffic; we aren't there).
- **CI health:** `CI & Deploy` and `Analytics snapshot` both green on every run back through
  08-09 — no cadence gaps this cycle. The 08-04/08-05 gaps AUDIT #3 flagged remain the standing
  unresolved pattern (no new occurrences to add).
- **Launch gate status:** 24 apps tracked (20 live + 4 pending-second-qa) vs. the ≥100-app
  gate — still far off, no gate decision to make this run.

## BUILD (backlog drain)
Resolved the recurring SERVICES enum gap first (LEARNINGS #45/46, unbuilt across FIND #11/#16/
#17/#18): added `ferretdb`, `pict-rs`, `soketi`, `mqtt` to the schema enum under a documented,
scoped policy (data-quality SKILL.md) — slots only for currently-queued candidates' cited
services, not speculatively for held/refuted ones (Sentry/PostHog's kafka/zookeeper/opensearch/
temporal stay unmapped). Unblocks Coolify, Wekan, Lemmy, Zigbee2MQTT for a future batch; not
built this run (pacing: one schema change per batch).

**Re-QA'd the 4 `pending-second-qa` apps** (Linkwarden, Chatwoot, Seafile, Mattermost) with an
independent fresh-eyes agent that did not build them. Zero defects across all 13 Known Defect
Classes — every source re-fetched live, every quote confirmed verbatim, every docker.size_mb
re-derived from a live registry manifest (all exact matches, no drift). **All 4 promoted to
`live`** (24 live total).

**Harvested a new 4-app batch** from the verified backlog queue: Jenkins (19/20), Keycloak
(18/20), Node-RED (18/20), GitLab CE (18/20) — the top-scoring queued items with no SERVICES
enum dependency. The harvester's canonical source domains (jenkins.io, keycloak.org,
nodered.org, docs.gitlab.com) were network-egress-blocked from its sandbox; it sourced content
from GitHub-mirrored raw docs but cited the unreachable canonical pages as `source_url` without
itself confirming they carried the same text — exactly the risk the harvester≠verifier
separation exists to catch.

**Independent verification** re-fetched every source: also egress-blocked on the canonical
domains, it repointed citations to self-confirmed raw GitHub mirrors instead of publishing an
unverified citation. Found and fixed a systematic error: all 4 apps' `docker.size_mb` was
computed as decimal MB (bytes/1,000,000) instead of this dataset's established binary MiB
convention (bytes/1,048,576) — corrected all 4 (verified independently by the orchestrating
session against gitea.json's live byte count before accepting the fix). Adjudicated GitLab CE's
deps: confirmed via GitLab's own docs mirror that the single-container Docker/Omnibus install
path bundles Postgres+Redis internally (not external services) — `deps: none` is correct;
separately fixed a `required:true` on a `service:"none"` entry to `required:false` to match
every other zero-dep app's convention. Moved all 4 to `pending-qa`.

**Independent QA** (did not harvest or verify) re-derived every figure live from scratch. 3 of 4
clean. Found one real defect: Keycloak's `deps` listed only PostgreSQL as `required:true`, but
Keycloak officially supports MariaDB/MySQL/Oracle/MSSQL too (`databases.adoc`) — `db.adoc`'s
Postgres example isn't exclusivity. Fixed to list postgresql/mysql/mariadb all `required:false`,
matching the dataset's existing multi-backend convention (confirmed against grafana.json and
nextcloud.json before applying). Per the unattended-run rule, all 4 land at `pending-second-qa`
(not live) even after passing QA — next run's fresh eyes give the final settle.

## Evidence
- Tests: **49/49 pass** at every checkpoint (session start 45/45 before the batch existed, then
  49/49 after each stage), `node --test tests/*.test.mjs`.
- Commits (chronological): `57e723f` (ANALYZE + SERVICES enum resolution + session recovery),
  `63f651c` (BUILD: re-QA settles Linkwarden/Chatwoot/Seafile/Mattermost to live), `e500512`
  (BUILD WIP: harvest Jenkins/Keycloak/Node-RED/GitLab CE), `df686a4` (BUILD WIP: independent
  verification fixes citations + docker.size_mb + GitLab CE deps), `3a3eb07` (BUILD: independent
  QA fixes Keycloak deps, all 4 to pending-second-qa).
- Dataset: 28 entries tracked — 24 live (+4 this run), 4 pending-second-qa (Jenkins, Keycloak,
  Node-RED, GitLab CE, next run's fresh-eyes settle them).
- Governance: LEARNINGS.md compacted (3 graduated entries shrunk to Compacted one-liners) to
  make room for 2 new entries (#53 docker.size_mb divisor, #54 SERVICES enum resolution), still
  under budget (7901/8000). DECISIONS.md and backlog/opportunities.md updated, both under
  budget (7589/8000, 9593/10000).
- Blocked, not built: nothing new blocked this cycle. Formbricks' `hub`/`cube` deps still
  unresearched — deliberately not added to the enum blind (SKILL.md policy: evidence first).

## LEARNINGS added (Wed cycle)
#53 (docker.size_mb harvester error was systematic decimal-vs-MiB, not a one-off — graduated
same-run into a tightened Known Defect Class #1). #54 (SERVICES enum gap resolved via a scoped
policy, not blanket expansion). Compacted: #46 (SERVICES enum, resolved), #49/#50 (both fully
CI-enforced, AUDIT #2/#3) shrunk to one-liners.

---

# Sun 2026-08-16 — ANALYZE + BUILD (specs-loop)

## Session-start recovery
Local `main` was again detached HEAD at the correct tip (`c9ab71e`, matching `origin/main`
exactly — no divergence). `git checkout -B main origin/main` re-pointed it; same recurring
pattern (LEARNINGS #32/#39 lineage), no data at risk.

## ANALYZE
Pre-launch phase (OPERATIONS.md §4) still applies: dataset readiness is the metric, not traffic.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs,
  confirmed live via GitHub API) — unchanged since 08-12, expected pre-launch.
- **Site traffic:** `reports/stats/total.json` (snapshot 2026-08-15T21:29 UTC) shows ~20 hits
  over the last 30 days, 1-4/day — consistent with operator/owner checks only. No collection-page
  or column kill/promote decision is due (SEO-tail gate: no kills before week 10 of real
  traffic).
- **CI health:** `CI & Deploy` green on every run 08-11 through 08-15 (confirmed via workflow-run
  history) — no new cadence gaps. FIND #21/#22 (08-14/15) were expected null cycles per LEARNINGS
  #56/#57 (opportunities.md at its byte ceiling), not silent failures — both committed a report
  of the block, so the cadence itself held even though mining was skipped.
- **Launch gate status:** 32 apps tracked (28 live + 4 pending-second-qa) vs. the ≥100-app gate
  — still far off, no gate decision to make this run.
- **Backlog ceiling (LEARNINGS #57):** resolved as a side effect of this run's drain, not by an
  architecture change — see BUILD below and LEARNINGS #58. Flagged to owner as deferred, not
  closed: if the ceiling recurs within a few more cycles, the archive/per-candidate-file
  question needs an actual decision, not another lucky drain.

## BUILD (backlog drain)
**Fresh-eyes re-QA'd the 4 `pending-second-qa` apps** (Jenkins, Keycloak, Node-RED, GitLab CE)
with an independent agent that did not build them. Jenkins/Keycloak/Node-RED clean on all 13
Known Defect Classes. GitLab CE had one real defect: `docker.size_mb` was 1319, independently
re-derived two ways (Docker Hub API + registry manifest layer-sum) as 1313 — fixed. Also
repointed its `deps[0].source_url` from a page that doesn't actually discuss dependencies to the
`requirements.md` section that does. All 4 promoted to **live** (28 live total).

**Harvested a new 4-app batch** from the verified queue, picked for score + simplicity (no
unresolved schema blockers): Coolify (20/20), Prometheus (18/20), docker-mailserver (18/20),
Wazuh (17/20). Notable finds: Wazuh's backlog note expected Docker tag `:5.1.0`, but that tag
doesn't exist on Docker Hub (only through `5.0.0-beta4`) — harvested against the real latest
stable tag `v4.14.7` instead. Coolify's 2GB/2-core figure confirmed control-plane-scoped (what
Coolify itself needs, not what it deploys) — a separate anecdotal 8GB/4-core example from one
named user was correctly NOT promoted to a recommended figure (Defect Class #2).

**Independent verification** re-fetched every source live, independently re-derived every
`docker.size_mb` (binary MiB, cross-checked via two methods for docker-mailserver's GHCR image),
and independently confirmed the Wazuh tag deviation was correct (queried Docker Hub's tag list
directly). Zero defects found — no fixes needed, all 4 moved to `pending-qa`.

**Independent QA** (did not harvest or verify) re-derived figures and `docker.size_mb` again
from scratch for all 4 (not a sample), and ruled on a flagged judgment call: Wazuh's `deps` is
`service: none` even though it bundles an OpenSearch-based indexer — ruled correct (not Defect
Class #12) because the recommended figure is scoped to the whole 3-container bundle, so no
resource-planning gap is hidden; declined to add an `opensearch` enum slot since the documented
scoped-extension policy doesn't license it here (new precedent, LEARNINGS #59). Zero defects
found on all 4. Per the unattended-run rule, all 4 land at **pending-second-qa** (not live) —
next run's fresh eyes give the final settle.

**Backlog housekeeping:** `backlog/opportunities.md` updated (Queued → In pipeline for the 4
built apps, Shipped count 24→28) and compacted from 9998 to 9080 bytes as a side effect of the
built entries' detailed write-ups shrinking into one-liners — this is what relieved the
FIND-blocking ceiling (LEARNINGS #58).

## Evidence
- Tests: **53/53 pass** at every checkpoint (`node --test tests/*.test.mjs`), including after
  each of the 4 build-stage commits.
- Commits (chronological): `2193a9a` (fresh-eyes re-QA settles Jenkins/Keycloak/Node-RED/GitLab
  CE to live), `7daeaac` (BUILD WIP: harvest Coolify/Prometheus/docker-mailserver/Wazuh),
  `73f755d` (BUILD WIP: independent verification, zero defects), `013978a` (BUILD: independent
  QA, zero defects, all 4 to pending-second-qa) — plus this report's governance commit.
- Dataset: 32 entries tracked — 28 live (+4 this run), 4 pending-second-qa (Coolify, Prometheus,
  docker-mailserver, Wazuh, next run's fresh-eyes settle them).
- Governance: LEARNINGS.md at 7831/8000 (added #58/#59, compacted #56/#57 into #58). DECISIONS.md
  and backlog/opportunities.md both under budget after this run's updates.
- Blocked, not built: nothing new blocked this cycle. Remaining Queued items (Zammad, Ghost,
  Mastodon, Lemmy, Netdata, Portainer, Wekan, Directus, Supabase, Twenty CRM, Formbricks,
  Zigbee2MQTT, TriliumNext, SearXNG, Stirling-PDF, Metabase, Odoo CE, License column) carry
  forward untouched — pacing, not a blocker.

## LEARNINGS added
#58 (byte-ceiling relief was a drain side effect, not an architecture fix — deferred, not
closed). #59 (bundled multi-container deployments don't need an enum slot for a component that
merely resembles a known service, when the published figure scopes the whole bundle — Wazuh
precedent). Compacted #56/#57 (byte-ceiling saga) into #58.
