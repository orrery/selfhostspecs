# Week 2026-32 (Sun 2026-08-09) — ANALYZE + BUILD

## Session-start recovery
Local `main` was 1 commit behind `origin/main` (prior session ended on a detached HEAD after
FIND #15, 2026-08-08 23:48 UTC, and its branch ref was never fast-forwarded). `git merge-base`
confirmed `origin/main` was a direct descendant — origin already had the commit, so nothing was
actually lost, only the local ref was stale. Fast-forwarded (`git merge --ff-only`); push
reported "everything up-to-date," confirming origin already carried it. Standing session-start
check (LEARNINGS #32/#39-lineage) held.

## ANALYZE
Pre-launch phase (OPERATIONS.md §4) still applies: the metric is dataset readiness, not traffic.
- **Repo/community signal:** 0 stars, 0 forks, 0 open issues (github.com/orrery/selfhostspecs) —
  unchanged, expected pre-launch.
- **Site traffic:** `reports/stats/total.json` shows 25 hits in the last 30 days as of the
  2026-08-08 snapshot (daily counts low single digits, one 0-hit day 08-07) — consistent with
  operator/owner checks only, nothing traffic-driven to evaluate.
- **CI health:** `CI & Deploy` green on every run back to 07-31. `Analytics snapshot` green on
  every run too — the `--retry 3` fix (confirmed holding since 08-02) continues to hold.
- **Cadence gap, now a recurring pattern across two routines:** `specs-find` produced no commit
  on 2026-08-04 (a second occurrence after the 07-28 gap AUDIT #2 flagged) and `specs-loop`
  itself produced no commit and no report entry for its Wed 2026-08-05 firing — the first known
  gap for this routine. `reports/2026-31.md` was also found mid-write, cut off at a bare
  `## BUILD` header with no content, from the 08-02 run. `list_triggers` exposes only
  `last_fired_at` (now overwritten by this run's own firing), not history, so cause can't be
  determined from here. Three gaps across two routines in ~2 weeks is a pattern, not a fluke —
  escalated to the owner below, LEARNINGS #44 written so future runs treat recurrence itself as
  the finding rather than re-discovering it as a surprise each time.
- **Launch gate status:** 24 apps tracked (18 live + 3 pending-second-qa + 3
  pending-verification) vs. the ≥100-app gate — still far off, no gate decision to make.

**Owner flag:** the scheduling-reliability pattern above (2 routines, 3 silent skips) is worth a
concrete decision — e.g. whether to add a lightweight heartbeat check, or whether this is a
known characteristic of the routine platform worth asking about directly — rather than another
silent log-and-continue.

## BUILD (backlog drain)
Per SKILL.md, re-QA'd everything already awaiting QA before anything else.

**Re-QA'd the 3 `pending-second-qa` apps** (Plausible CE, Linkwarden, Open WebUI) with a fresh,
independent QA subagent that did not build them. Plausible CE and Open WebUI: zero defects
across all 13 Known Defect Classes (sources re-fetched, docker image/size/arches confirmed via
live ghcr.io manifest) — **both promoted to `live`** (20 live total). Linkwarden: one real
defect — its RAM-figure `source_url`/`evidence_url`/`sources_of_truth` all cited
linkwarden/docs' `installation.md`, which now 404s (the repo restructured, page moved to
`setup.md`). The verbatim quote is unchanged and still live at the new location; repointed all
three citations, added the same-day changelog entry OPERATIONS.md requires for a moved source,
and tightened scope wording (the hardware section precedes the doc's Docker Compose/Manual
Installation branch, so it's a general figure, not Docker-specific). Kept `pending-second-qa`
per the unattended-run rule (a fix, not a from-scratch clean pass).

**Independently verified the 3 `pending-verification` apps** (Chatwoot, Seafile, Mattermost —
harvested 08-02 from FIND #7, never verified). Chatwoot and Mattermost: zero discrepancies.
Seafile: one minor scope-wording issue — `ram_min_mb`/`cpu_min_cores` said "Docker-based
deployment" but the source's own preamble states the figures apply to all deployment scenarios
unless otherwise specified; corrected. All 3 moved to `pending-qa`, then given an independent
QA pass by a separate subagent (re-checked the Seafile fix with active skepticism, confirmed it
holds up against the source's own Docker-vs-All split on its Pro tier) — zero defects on all
three. **All 3 now `pending-second-qa`**, next run's fresh eyes settle them.

No new apps harvested this cycle — capacity went to draining the existing pending-verification
and pending-second-qa queue (7 apps touched) plus the session-start recovery and cadence
investigation above. The 20+ items still `queued` in the verified backlog (GitLab CE, Zammad,
Ghost, Mastodon, Lemmy, Netdata, Portainer, Wekan, Directus, Supabase, Twenty CRM, Formbricks,
Jenkins, Coolify, TriliumNext, SearXNG, Stirling-PDF, Keycloak, Metabase, Wazuh, License column)
remain for the next BUILD cycle.

## Evidence
- Tests: **45/45 pass** at every checkpoint (session start, after each fix, after final
  rebuild), `node --test tests/*.test.mjs`.
- Commits (chronological): `49dff69` (ANALYZE governance + orphaned-commit recovery), `774b698`
  (BUILD WIP: verification passes Chatwoot/Mattermost, fixes Seafile scope), `e172889` (BUILD:
  re-QA settles Plausible CE + Open WebUI to live, fixes Linkwarden dead link), `caec873`
  (BUILD: independent QA clears Chatwoot/Seafile/Mattermost to pending-second-qa).
- Recovered orphaned commit `7cd2275` (FIND #15) onto local `main`; confirmed already on
  `origin/main` (no data loss, stale local ref only).
- Dataset: 20 live (+2 this run), 4 pending-second-qa (Linkwarden, Chatwoot, Seafile,
  Mattermost), 0 pending-verification = 24 entries tracked.
- Governance: DECISIONS.md and LEARNINGS.md compacted (redundant/graduated entries shrunk) and
  this run's findings appended, both back under byte budget; backlog/opportunities.md updated
  and still under its 10000-byte budget (9995).
- Blocked, not built: nothing new blocked this cycle (no new harvest attempted).

## LEARNINGS added
#44 (recurring cadence gap across `specs-find` and `specs-loop` — escalate the pattern, not
each instance). #39 removed (fully graduated into the existing Compacted merge-base bullet);
#41 shrunk to one line (graduated into Known Defect Class #13).
